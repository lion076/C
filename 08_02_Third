#include <stdio.h>

int main(void)
{
	int x, n, a, b;
	int total = 0;

	scanf("%d\n", &x);
	scanf("%d\n", &n);

	for (int i = 0; i < n; i++) {
		scanf("%d %d", &a, &b);
		total += a * b;
	}

	if (x == total) {
		printf("Yes");
	}
	else {
		printf("No");
	}

	return 0;
}
